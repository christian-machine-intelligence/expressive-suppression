# reproduce the paper's tables and figures from the shipped results (CPU)
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
# re-run the experiment (GPU)
torch>=2.10
transformers>=5.14
peft>=0.14
anthropic>=0.60
